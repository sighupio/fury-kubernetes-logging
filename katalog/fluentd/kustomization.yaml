namespace: logging

resources:
  - deploy.yml
  - deploy-fluentbit.yaml
  - rbac.yml
  - sm.yml

images:
  - name: quay.io/sighup/fluentd-elasticsearch
    newTag: v1.10.2-debian-1.0

configMapGenerator:
- name: fluentd-conf
  files:
    - fluent.conf


secretGenerator:
  - name: fluentbit
    files:
      - fluent-bit.conf