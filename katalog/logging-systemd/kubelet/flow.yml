apiVersion: logging.banzaicloud.io/v1beta1
kind: Flow
metadata:
  name: systemd-kubelet
spec:
  filters:
    - dedot:
        de_dot_separator: "_"
        de_dot_nested: true
  match:
    - select:
        labels:
          app.kubernetes.io/instance: systemd-kubelet-host-tailer
  localOutputRefs:
    - elasticsearch-systemd-kubelet